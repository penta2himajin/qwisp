# Python
__pycache__/
*.pyc

# Step 1/2 trace outputs (can be large) — prompts*.jsonl は追跡する
traces*.jsonl
*.traces.jsonl

# ベンチ由来の生成プロンプト（manifest+lock で再現可能・データ再配布回避）
tools/step1_routing_trace/prompts.bench.jsonl

# 長文脈の実素材（各自のコード/ログ）— README だけ追跡
tools/step1_routing_trace/corpora/*
!tools/step1_routing_trace/corpora/README.md
