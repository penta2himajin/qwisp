# Python
__pycache__/
*.pyc

# Step 1/2 trace outputs (can be large) — prompts*.jsonl は追跡する
traces*.jsonl
*.traces.jsonl
results*.json

# ベンチ由来の生成プロンプト（manifest+lock で再現可能・データ再配布回避）

# 長文脈の実素材（各自のコード/ログ）— README だけ追跡

# generated measurement refs (reproduce via qwisp/bench_refs.py)
/refs/
.claude-glm-task.md

# macOS
.DS_Store
*.profraw
